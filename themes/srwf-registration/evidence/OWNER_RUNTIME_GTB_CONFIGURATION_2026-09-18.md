# Owner Runtime Evidence — GTB Per-Form Configuration — 2026-09-18

This record captures the Owner-supplied runtime evidence for the first SRWF Registration per-form GTB Theme configuration batch.

It is runtime evidence, not visual-design authority. `reference/` remains the only visual-authority source.

## Evidence identity

Owner-supplied artifacts and SHA-256:

| Artifact | SHA-256 |
|---|---|
| `gtb-srwf-runtime-diagnostic-v0.3.0 (8).json` | `d3e0aee6b59b4b3ee9f8ae2d7d640b70068f56bfa48802875f2decbb12ec1480` |
| `gtb-srwf-admission-diagnostic-v0.3.2 (4).json` | `1cce85819bf4e3d3f064f275c393a9962dd33512c41dfe63fb4abbe70b29d1a0` |
| Registration-page screenshot `screencapture-kanoonshiraz-ir-2010-2-2026-09-18-19_27_58.jpg` | `20184898682f818d8107def53e55eb29db2f4a14b5a0050573b303f7470d91ae` |
| GTB Theme settings screenshot `screencapture-kanoonshiraz-ir-wp-admin-admin-php-2026-09-18-19_30_00.jpg` | `ea37eef5a6eb70ae2ade539b8e31c8d234db7c4efc1ad26bd9722100a2704593` |

The original Owner artifacts were supplied out-of-repository. This repository record preserves their exact identities and the bounded claims derived from them; it does not substitute regenerated bytes for those originals.

## Proven GTB settings behavior

The Owner admin screenshot shows the native Gravity Forms per-form settings surface at **Settings → GTB Theme** after the permission-gate repair.

Observed state:

- save notice: `GTB Theme configuration was saved and GTB-owned presentation tokens were projected.`
- current status: `READY`;
- current saved theme profile: `SRWF Registration / Enabled`;
- all eight semantic mappings selected and reported `READY`.

Observed mappings:

| Semantic role | Runtime field/section reference | GTB token |
|---|---:|---|
| Gender | `#92` | `srwf-role-binary-choice` |
| Graduation Status | `#205` | `srwf-role-binary-choice` |
| Report Card upload | `#208` | `srwf-role-report-card-upload` |
| Identity Section Break | `#180` | `srwf-role-section-identity` |
| Contact Section Break | `#181` | `srwf-role-section-contact` |
| Education Section Break | `#182` | `srwf-role-section-education` |
| School/Documents Section Break | `#183` | `srwf-role-section-school-documents` |
| Student Photo Section Break | `#184` | `srwf-role-section-student-photo` |

These numeric IDs are evidence references for this exact Owner form only. They are not production selectors, public theme identity, or a durable cross-form contract.

## Runtime diagnostic proof

`gtb-srwf-runtime-diagnostic-v0.3.0 (8).json` reports:

- `collectorFailures: []`;
- one SRWF target;
- wrapper classes include `gform-theme--foundation`, `gform-theme--framework`, `gform-theme--orbital`, and `srwf-registration-theme_wrapper`;
- form classes include `srwf-registration-theme`;
- `srwf-registration-theme-css` is present at stylesheet index `16`, path `/wp-content/plugins/gtb-srwf-registration/srwf-registration.css`, version `0.1.6`;
- the GTB stylesheet follows the Gravity Forms Reset/Foundation/Framework/Orbital styles in the observed order;
- all eight configured semantic roles are present in runtime field signatures on the same field/section references recorded by the settings screenshot;
- the runtime canonical properties include the admitted SRWF control, color, typography, button, file-zone, divider, and validation variables;
- the submit control is observed at `840×56` in this capture.

The field-signature evidence specifically contains:

- `field_11_92` → `srwf-role-binary-choice`;
- `field_11_205` → `srwf-role-binary-choice`;
- `field_11_208` → `srwf-role-report-card-upload`;
- `field_11_180` → `srwf-role-section-identity`;
- `field_11_181` → `srwf-role-section-contact`;
- `field_11_182` → `srwf-role-section-education`;
- `field_11_183` → `srwf-role-section-school-documents`;
- `field_11_184` → `srwf-role-section-student-photo`.

This proves the per-form Save path persisted the bounded GTB configuration and projected the intended owned semantic tokens into the authentic Gravity Forms runtime.

## Admission proof

`gtb-srwf-admission-diagnostic-v0.3.2 (4).json` reports two non-truncated decisions with:

- `formIdentityMatched: true`;
- `presentationAdmitted: true`;
- `renderingContext: registration`;
- `contextEvidence: registration_default`;
- `exclusionReason: null`;
- `srwfStylesheetPresent: true`.

This proves the configured form is admitted as Registration presentation in the captured Registration runtime and the SRWF production stylesheet is present there.

## Screenshot-level evidence

The Registration screenshot is consistent with the configured theme being active after Save and shows the authentic Gravity Forms form with the SRWF section/icon, control, upload, and submit presentation in the real site shell.

The screenshot is not used to promote unresolved visual values or to claim pixel-perfect equality with the admitted visual artifact.

## What this closes

For this exact Owner runtime, the following per-form GTB configuration claims are now `RUNTIME_PROVEN`:

- the GTB Theme settings page is reachable through the native Gravity Forms form-settings surface after the native permission-resolver repair;
- explicit Save succeeds in the real host;
- the bounded configuration persists;
- the saved profile reports Enabled;
- all eight required semantic slots can be mapped to real compatible host objects;
- readiness reaches `READY`;
- the activation token is present on the authentic form;
- all seven distinct owned role-token strings are projected onto the expected eight real field/section consumers;
- Registration presentation is admitted and the `0.1.6` stylesheet is present.

## What this does not close

This evidence does **not** by itself prove:

- Gravity Flow Entry Detail exclusion on this new Owner pass; that remains governed by the separate PR #13 runtime evidence and regression tests until a new Entry Detail capture is supplied;
- approved desktop/mobile visual equivalence across all states;
- keyboard/focus behavior;
- validation/error states;
- text enlargement / 320 CSS px reflow;
- authentic conditional reveal behavior for Graduation Status in every path;
- Student Photo post-upload/crop/re-crop/delete presentation;
- PersianGravity Jalali picker presentation;
- unresolved production breakpoint, desktop short-field pairings, desktop shadow, title metrics, exact focus ring, helper/error exact sizing, or field/section rhythm.

Overall SRWF Registration production qualification therefore remains open even though the per-form GTB configuration/save/projection path is now runtime-proven.
